{
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "build": "pnpm -r --filter ./packages run build",
    "lint": "eslint '**/*.{js,ts,vue}'",
    "lint:fix": "nr lint --fix",
    "link": "pnpm -r --filter ./packages run link",
    "release:theme": "pnpm --dir packages/vitepress-theme run release",
    "release:shared": "pnpm --dir packages/shared-components run release"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.6.6",
    "@types/codemirror": "^5.60.1",
    "@types/lz-string": "^1.3.34",
    "@types/node": "^15.14.0",
    "@types/prismjs": "^1.16.5",
    "eslint": "^7.29.0",
    "typescript": "^4.3.5",
    "vite": "^2.3.8"
  }
}
